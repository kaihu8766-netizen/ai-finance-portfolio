# 2026-09-11 作品集迭代问题记录与反思

## 今日概述

今天主要在解决手机端作品集的显示问题，过程中遇到了多个连锁问题，一度越改越乱，最终通过回退到稳定版本+只修关键问题的方式恢复稳定。本文档记录完整过程，供后续参考和DeepSeek复核。

---

## 问题一：手机端图表加载失败

### 现象
手机端打开作品后，图表区域显示"图表加载失败，请刷新重试"，电脑端正常。

### 根因
手机端适配代码 `applyMobileOption()` 使用 `Object.assign` 合并配置，但没有正确处理**数组类型**的 `grid`/`xAxis`/`yAxis`。当这些属性是数组时（多grid图表），`Object.assign` 会把数组当对象合并，导致配置损坏，ECharts初始化失败。

### 修复过程（走了弯路）
1. 加 `try-catch` 错误保护 → 没用，配置已经损坏
2. 加详细错误信息显示 → 能看到错误但没解决
3. 暂时禁用手机端适配 → 图表能显示了
4. 重新写"安全版"适配，只做必要调整 → 重新启用后又出问题
5. 最终决定：**手机端适配保持禁用**，用原始配置确保图表显示

### 教训
- ECharts配置合并必须处理数组类型，不能简单Object.assign
- 更好的方案是用ECharts原生的 `media` query功能，在option里定义响应式配置，而不是JS手动合并
- 适配逻辑有问题时，先禁用确保基本功能，不要在错误基础上继续叠加

---

## 问题二：isMobile is not defined

### 现象
禁用适配后重新启用时，图表显示"错误: isMobile is not defined"。

### 根因
作品详情页是在 **iframe** 里通过 `srcdoc` 动态加载的。`isMobile()` 和 `applyMobileOption()` 函数定义在主页面脚本里，但**没有被注入到iframe**，所以iframe里调用时报未定义。

### 修复
1. 把这两个函数移到单独的 `<script id="mobile-utils">` 标签
2. 在 `showWork()` 函数里，把mobile-utils的内容注入到iframe的HTML中
3. 这样iframe里就能正常访问这两个函数

### 教训
- iframe是独立的执行环境，主页面定义的函数不会自动可用
- 动态生成iframe内容时，必须把依赖的函数/样式都注入进去
- 做iframe相关功能时，先想清楚"哪些东西需要注入"

---

## 问题三：打开作品显示在底部

### 现象
打开LPR作品（w03），但最顶部显示的是"05 局限与诚实披露"，而不是01章节。用户以为是章节顺序错了，实际是滚动位置问题。

### 根因
iframe的滚动位置没有重置。用户从上一个作品（比如w01）滚动到底部，返回主页后再打开新作品，iframe里的滚动位置还是上一个作品的底部。

### 修复
在iframe的body标签加 `onload="window.scrollTo(0,0)"`，每次加载完成后自动滚动到顶部。

### 教训
- iframe切换内容时，滚动位置不会自动重置
- 不要假设用户看到的"顶部"就是内容的顶部，先检查滚动位置
- 动态内容加载后，显式重置滚动位置是好习惯

---

## 问题四：新旧作品内容叠加

### 现象
打开w03作品时，页面上半部分是w01的表格底部，中间是"返回作品集"按钮，下半部分是w03的顶部——两个作品的内容混在一起了。

### 根因
从一个作品直接切换到另一个作品时，`showWork()` 直接设置 `iframe.srcdoc = 新内容`，但浏览器（尤其是移动端）可能不会完全重新加载，导致**旧内容残留**，新旧内容叠加。

### 修复
```javascript
// 先清空旧内容
iframe.srcdoc = '';
// 延迟50ms再设置新内容，确保浏览器完全清理旧内容
setTimeout(function(){
  iframe.srcdoc = html;
}, 50);
```

### 教训
- iframe的srcdoc更新不是即时的，浏览器可能缓存旧内容
- 动态更新iframe内容时，先清空再设置，加短暂延迟确保完全刷新
- 移动端浏览器对iframe的处理和桌面端不一样，更容易出问题

---

## 问题五：GitHub语言比例HTML过高

### 现象
GitHub仓库语言统计显示 HTML 96.8%，JavaScript 1.8%，Python 1.3%，用户觉得HTML比例太高，不够真实。

### 根因
`versions/` 文件夹里有 **38个历史版本HTML文件，共95347行**，而根目录的index.html只有5730行。历史版本把HTML比例拉高了。

### 修复
在 `.gitattributes` 里把 `versions/*` 标记为 `linguist-vendored`，不计入语言统计。修复后大致比例：
- HTML: 5730行（只有当前版本）
- Python: 5488行
- SQL: 152行

### 注意
GitHub语言统计是异步计算的，修改后可能需要几小时才能看到变化。

### 教训
- 历史归档文件会影响语言统计，需要标记为vendored
- 新增Python/SQL文件时，要考虑和HTML的行数比例，少量文件改变不了大比例

---

## 最终决策：回退到稳定版本

### 为什么回退
今天的修改越叠越多，问题一个接一个：
1. 手机端适配 → 图表加载失败
2. 修复适配 → isMobile未定义
3. 修复未定义 → 打开作品显示在底部
4. 修复滚动 → 新旧内容叠加

每个修复都引入了新问题，继续在当前版本上改风险太大。

### 回退方案
1. `git reset --hard 4030f40` 回退到"禁用手机端适配"的稳定版本
2. 在稳定版基础上只修两个关键问题：iframe内容叠加 + 自动滚动顶部
3. 手机端适配保持禁用，之后单独慢慢做

### 结果
稳定版本已部署，基本功能正常：
- 作品能正常打开/关闭
- 图表能正常显示（原始配置）
- 作品切换不会内容叠加
- 打开作品从顶部开始

---

## 深度反思

### 1. 不要在不稳定的基础上继续叠加
今天最大的教训：当一个修复引入新问题时，应该停下来回退，而不是继续打补丁。打补丁的结果就是问题越来越多，越来越难定位。

### 2. iframe是复杂环境，要谨慎
作品集用iframe加载作品详情，这个设计带来了很多隐性问题：
- 函数需要注入
- 滚动位置需要重置
- 内容更新需要先清空再设置
- 移动端浏览器行为不一致

如果重新设计，可能考虑不用iframe，直接在主页面里切换显示内容。

### 3. 手机端适配的正确姿势
不要用JS手动合并ECharts配置。正确的做法是用ECharts原生的media query：
```javascript
option = {
  baseOption: { /* 电脑端配置 */ },
  media: [
    { query: { maxWidth: 768 }, option: { /* 手机端配置 */ } }
  ]
};
```
ECharts自动根据屏幕宽度切换，不会有配置合并的问题。

### 4. 详细错误信息很重要
今天能快速定位"isMobile未定义"，就是因为加了详细错误信息显示。如果只显示"图表加载失败"，根本不知道是什么问题。

### 5. 每次只改一个问题
今天的问题部分原因是一次改了太多东西（适配函数+isMobile+注入+滚动），出问题后不知道是哪个改动引起的。以后应该：改一个→验证→再改下一个。

### 6. Git是救命稻草
`git reset --hard` 能快速回退到稳定版本，这是今天能恢复稳定的关键。频繁提交、写清楚commit message很重要。

---

## 后续待办

- [ ] 手机端图表适配：用ECharts原生media query重新实现，不手动合并配置
- [ ] 考虑是否去掉iframe，改用主页面内切换内容
- [ ] 手机端表格布局优化（数据来源表格在手机上显示拥挤）
- [ ] DeepSeek复核：让DeepSeek看看今天的问题记录，给出改进建议
