# 第十九次讨论·主页作品卡片Demo入口设计讨论

## 背景

用户反馈：作品集有4个可运行Demo，但是入口藏得太深（在作品详情页里面），很多人浏览主页时找不到Demo，也不知道这些作品是可以交互的。

**目标**：在主页对应的作品卡片内加入显眼的Demo入口，让用户一眼就能看到"这个作品可以玩"，并且点击就能直接打开Demo。

## 现状

### 4个有Demo的作品
| 作品 | Demo文件 | 位置 |
|------|----------|------|
| 作品07（核心） | demo/现金流压力测试模拟器.html | 作品详情页底部 |
| 作品05（核心） | demo/供应链现金流压力测试模拟器.html | 作品详情页底部 |
| 作品06（核心） | demo/SOX控制测试工作台.html | 作品详情页内 |
| 作品03（mini） | demo/LPR蒙特卡洛预测模拟器.html | 作品详情页底部 |

### 1个没有Demo的作品
- 作品01（第三方支付资金渠道研究）—— 纯研究型，没有交互Demo

### 主页作品卡片结构
- 3个宽卡（作品07/05/06）：badge + title + desc + core-flag
- 2个mini卡（作品01/03）：badge + title + desc + meta（时间/标签）
- 整个卡片是`<a>`标签，点击跳转到作品详情页

## 设计方案（4种）

### 方案A：描述下方加明显链接
在作品描述（desc）下面加一行：
```html
<div class="demo-entry">🚀 可交互Demo →</div>
```
- 样式：渐变背景或强调色，padding 8px 12px，border-radius 8px，font-weight 600
- 位置：desc下面，卡片底部
- 交互：点击在新窗口打开Demo，阻止事件冒泡（不跳转作品详情页）

**优点**：显眼，用户一眼就能看到；不破坏现有布局；点击直接打开
**缺点**：占一点垂直空间；3个宽卡的布局可能需要微调

### 方案B：卡片右下角加小按钮
在卡片右下角加一个浮动的小按钮：
```html
<span class="demo-btn">🚀 Demo</span>
```
- 样式：小尺寸，圆角，半透明背景，hover时高亮
- 位置：卡片右下角，absolute定位
- 交互：点击在新窗口打开Demo

**优点**：紧凑，不占描述空间；不破坏卡片整体布局
**缺点**：可能不够显眼，用户可能注意不到；absolute定位在响应式下可能有问题

### 方案C：卡片右上角加"可运行"角标
在卡片右上角（或badge旁边）加一个角标：
```html
<span class="demo-badge">🔗 可交互</span>
```
- 样式：小徽章，和现有badge风格统一
- 位置：卡片右上角，或badge行的末尾
- 交互：点击在新窗口打开Demo

**优点**：不占空间，像徽章一样；和现有badge风格统一
**缺点**：用户可能不知道这个角标可以点击；不够显眼

### 方案D：meta区域加Demo标签
在mini卡的meta区域（时间/标签那行）加一个Demo标签：
```html
<span class="tag demo-tag">🔗 可运行Demo</span>
```
- 样式：和现有tag风格统一，但用强调色
- 位置：meta行的末尾
- 交互：点击在新窗口打开Demo

**优点**：和现有meta风格统一；不破坏布局
**缺点**：只适用于mini卡（宽卡没有meta区域）；可能不够显眼

## 需要DeepSeek讨论的问题

### 问题1：哪个方案最好？
从用户体验、视觉效果、实现复杂度三个维度，哪个方案最适合这个作品集？

### 问题2：有没有更好的方案？
有没有其他方式可以让Demo入口既显眼又不破坏布局？比如：
- 卡片hover时显示Demo按钮？
- 卡片上加一个"预览"缩略图？
- 主页单独加一个"可运行Demo"区域？

### 问题3：交互细节
- 点击Demo入口时，是否应该同时打开作品详情页？还是只打开Demo？
- 是否需要在Demo页面加一个"返回作品集"的按钮？
- 移动端的交互是否需要特殊处理？

### 问题4：文案
- "可交互Demo"这个文案好不好？有没有更好的？
- 选项："🚀 打开模拟器"、"🔗 可运行Demo"、"▶ 立即体验"、"🎮 交互演示"
- 哪个文案最能吸引用户点击？

### 问题5：作品01（没有Demo）怎么处理？
- 方案A：什么都不加，保持原样
- 方案B：加一个"📄 研究报告"的入口（如果有PDF的话）
- 方案C：加一个"🔬 研究型"的标签，说明这是纯研究作品

## 技术约束

1. 作品卡片本身是`<a>`标签，点击跳转到作品详情页
2. 在卡片内加Demo入口，需要阻止事件冒泡（`event.stopPropagation()`），否则点击Demo会同时触发作品详情页跳转
3. Demo链接需要`target="_blank"`，在新窗口打开
4. 需要考虑响应式：移动端卡片布局可能变化
5. 3个宽卡 + 2个mini卡的布局不同，需要分别处理

## 参考

- 现有作品详情页的Demo按钮样式：渐变背景，圆角，font-weight 600，box-shadow
- 现有卡片的badge样式：小圆点 + 文字，border，半透明背景
- 现有mini卡的meta/tag样式：小字体，圆角，半透明背景

---
*报告生成时间：2026-09-21*
*主力AI：豆包（WorkBuddy）*
*待讨论AI：DeepSeek Harness*
*讨论主题：主页作品卡片Demo入口设计*
