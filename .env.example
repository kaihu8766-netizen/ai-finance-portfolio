# 本地环境变量模板（复制为 .env 后填写真实值）
# ⚠️ .env 已被 .gitignore 排除，禁止提交；真实值绝不写入本文件或任何入库文件

# DeepSeek API（调用 deepseek_gate.py / deepseek_client.py 用）
# 获取：platform.deepseek.com → API Keys
# 说明：仅本地使用；CI 侧使用 GitHub Actions Secrets（见 trace/SECRETS_GUIDE.md）
DEEPSEEK_API_KEY=sk-你的真实key填写在这里

# 可选：GitHub Token（本地审计/推送用，用完即删，勿长期保留）
# GITHUB_TOKEN=ghp_xxx
