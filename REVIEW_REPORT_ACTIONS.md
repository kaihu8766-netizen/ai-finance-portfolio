# REVIEW_ACTIONS — 需要动手的事

> 复核AI（DeepSeek Harness）｜完整报告 `REVIEW_REPORT_v6.md`（**§35 = 本轮：第十五次讨论·作品集方向建议；§34 = 简历方案；§33 = 主页文字 + 流程**）
> # 🚦 第三十二轮：对方建议 **3 分真有用 / 2 分是套路**；你已修了 Demo 数字与 3 处品牌写法；**仍待处理：7 处 `smart`、master 未批准**

## 🔴 一、先处理还没做的（上一轮遗留）

| # | 事项 | 现状 | 改法 |
|---|---|---|---|
| 1 | **master 处于"内容未批准"** | `python tools/check_review_stamp.py` → **exit 1**（最新批准行仍是 `814f782`，而 PR #5/#6 已并入 master） | ① 事后批准 + 违规记录（同 `ec94b0e` 先例）；或 ② revert。**根治**：GitHub 把 `Review gate` 两个 job 设为 **required status checks** |
| 2 | **品牌写法仍有 7 处 `smart`** | `:2194`（KB 作者）、`:2199`（助手话术）、`:3135`（作品01 页头）、`:3665`（作品03 页头）、`:4354`（作品05 页头）、`:4362`、`:4415`（作品05 正文）<br>（`:2196` 的 KB 关键词 `'smart'` **要保留** = 兼容别名） | 统一为 `smart（奔驰×吉利）`（`:1558`/`:1604`/`:2691` 已改 ✓） |
| 3 | `.preview/` 未跟踪也未忽略 | `git status` 里显示 `?? .preview/` | 要么提交、要么加进 `.gitignore`（避免误提交） |

## 🎯 二、本轮结论（第十五次讨论 · 6 个问题的答案）

### Q1 对方的批评中肯吗 → **只有一条中肯：真实数据缺位**
- "停留在前端 HTML" ⚠️ **事实对、定性错**：你是静态前端没错（单文件 + 4 个 demo + Python），但**财务岗不用"前端/后端"这把尺子** —— 看的是 ① 懂不懂业务痛点 ② 判断有没有逻辑链和出处 ③ 有没有工程化意识
- 你**有别人没有的第三点**：双 Agent 复核 → **复核凭证 + CI 闸门 + 10 项 preflight + 41 题回归测试**，仓库里可当场核验
- 对方自相矛盾：一边说"停留在前端"，一边说"实习走不完链路是正常的"

### Q2 优先级 → **先修旧的，再加新的**
| 优先级 | 事项 | 成本 | 收益 |
|---|---|---|---|
| **P0** | 修不一致（7 处 smart + master 未批准） | 0.5 天 | ⭐⭐⭐⭐⭐ |
| **P1** | 每个作品加**「数据口径与假设」表**（不是"企业级数据字典"） | 1 天 | ⭐⭐⭐⭐⭐ |
| **P2** | 补齐**决策链**（输入→判断→动作）为标准段落 | 1–2 天 | ⭐⭐⭐⭐ |
| **P3** | **痛点量化**（讲清代价：每次几小时/每月几次/返工） | 0.5 天 | ⭐⭐⭐⭐ |
| **P4** | （可选）Dify/工作流项目 —— **只在 JD 要求时做** | 3–7 天 | ⭐⭐⭐ |
| **P5** | 企业级数据字典 / 全栈开发 | 数周 | ⭐ **不建议** |

### Q3 Dify → **看 JD**；要做就选贴你现有资产的场景
推荐顺序：① **银行流水/台账自动核对**（贴你的实习）② **SOX 控制点证据自动核对**（差异化最强，复用作品06）③ 发票/报销审核 ④ 合同条款抽取。
判断"是否不务正业"的标准：**表述落在业务价值上**（"规则自动核对 + 例外交人，人工时间↓60%，规则可审计"），不是"我会用 Dify"。

### Q4 数据字典 → **"企业级"是套路，"口径表"是刚需**
你已有素材（作品01 多口径差异分析、w05 数据核验清单、w03/w07 数据来源说明）→ 统一成每个作品一张 **「数据口径与假设」** 表（字段 → 口径定义 → 来源 → 粒度 → 偏差/假设 → 可核验性），5–8 行即可，**放在洞察章节之前**。模板见 `REVIEW_REPORT_v6.md` §35.5。
另：w05 的**桑基图**就是对方要的"业务链价值图" ✓ → 只需补一句"这张图回答什么决策问题"。

### Q5 核心竞争力 → **主打"AI 产出可核验"，不要主打精算、不要主打"会用 AI"**
> 一句话：**"我能把财务业务问题翻译成可核验的数据/模型方案，并且知道怎么让 AI 的产出不出错。"**
稀缺度：① AI 产出可核验的工程化意识 ⭐⭐⭐⭐⭐ ② 业务翻译能力 ⭐⭐⭐⭐ ③ 量化建模（精算）⭐⭐⭐⭐（会被归到保险岗，别当主打）

### Q6 面试叙事 → 你的 5 步骨架对了，但**缺第 5 步"我怎么保证它可信"**
① 场景一句话 → ② 痛点的代价（量化）→ ③ 方案与关键假设 → ④ 结果与边界（数字 + 主动说局限）→ **⑤ 可核验机制（双 Agent + 复核凭证 + 闸门）**。
逐字稿与"避免学生 demo 感的 5 招 + 3 个必被追问的问题"见 §35.7。

### 对方建议"真有用 vs 套路"速查
✅ 有用：业务链价值图（补一句决策问题）· 模拟真实场景（守"数字不编"边界）· "上手就能干活"（当面试开场）
❌ 套路：企业级数据字典 · 全栈开发 · 为 Dify 而 Dify（只教工具不教业务深度）

## ✅ 顺手已确认修好的
- 主页统计卡 **`4 可运行 Demo`**（与 `demo/` 4 个 HTML 一致）✅
- `:1558`/`:1604`/`:2691` 品牌写法已改 `smart（奔驰×吉利）` ✅

## 读报告
```bash
git show HEAD:REVIEW_REPORT_ACTIONS.md
git show HEAD:REVIEW_REPORT_v6.md | sed -n '4470,4628p'   # §35 完整回复（6 问 + 面试逐字稿 + 对照表）
```

---

*复核AI（DeepSeek Harness）生成。本轮：建议 3 真 2 套；待办 = 7 处 smart + master 未批准 + （可选）Dify/口径表/决策链/痛点量化。*
