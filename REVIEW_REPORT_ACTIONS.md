# REVIEW_ACTIONS — 需要动手的事

> 复核AI（DeepSeek Harness）｜完整报告 `REVIEW_REPORT_v6.md`（**§36 = 本轮：数据口径表落地复核；§35 = 第十五次讨论；§34 = 简历方案；§33 = 主页文字 + 流程**）
> # 🚦 第三十三轮：**「数据口径与假设说明」表落地 ✅ 实施质量好**（5 个作品、6 列、逐表列数一致、表头统一）；剩 **5 处小残留** + **master 仍未批准**

## ✅ 一、本轮验收通过（实测证据）

| 项 | 证据 |
|---|---|
| **口径表 × 5 个作品** | 标题+徽标统一 `数据口径与假设说明 / DATA DEFINITIONS`（`:3618` w01、`:3878` w03、`:5015` w05、`:5228` w06、`:6005` w07） |
| **6 列结构一致性** | 逐表逐行统计单元格：**5 张表全部恰好 6 列**、无一行错位；表头 5 表**完全一致**（`字段/指标｜业务口径定义｜数据来源｜粒度/时间｜已知偏差或假设｜可核验性`） |
| **偏差栏写得具体且诚实** | 如"**改革前后口径不可比**"（LPR）、"**国有/股份/城商/农商差异较大，行业平均仅供参考**"（净息差）、"**缺陷分级需按企业实际与准则调整**"（SOX）—— 这正是财务岗最认可的"口径意识" |
| **桑基图补了"决策问题"** | 新增"这张图回答的决策问题：…哪一段资金占用最长、可以从哪里压出来" ✅（§35.5 的建议） |
| **顺手修掉一处重复短语** | w07 `模型方法` 行原来的 `股价/汇率随机游走模型（股价/汇率随机游走模型（几何布朗运动））` → 现在干净了 ✅ |
| **`smart` 统一** | `:1558`/`:1604`/`:2691`/`:3135`/`:3665`/`:4354`/`:4362`/`:4415` 均 `smart（奔驰×吉利）`；`:2194` KB 作者介绍用全称 `smart（梅赛德斯-奔驰 × 吉利合资）` ✅ |
| **响应式兜底** | `.portfolio-page table{display:block}`（`:629`）、`.app table{overflow-x:auto !important}`（`:803`）→ 6 列表格窄屏**横向滚动**，不会撑破布局 ✅（建议手机上人工看一眼） |
| **结构复检** | 内联脚本 **14 块 `node --check` 全过**；逐页编号 **全部 `01..N` 连续**、局限/口径**各 5 篇**；`<div>` 平衡 **−3**（未变）；`U+FFFD` 0 |
| **闸门** | `preflight` **0**（10 项全绿）· `coverage` **0**（41 题全绿）· `check_review_stamp` **1**（正确红） |

## ❌ 二、剩的 5 处（都很小）

| # | 问题 | 位置 | 改法 |
|---|---|---|---|
| 1 | 助手话术仍写英文 **`Smart`**（唯一漏改的可见处） | `index.html:2199` | 改成 `在 smart（奔驰×吉利）做资金实习时…` |
| 2 | **话术仍引用旧章节名** `数据来源与假设说明`（章节已改名） | `index.html:2204`、`index.html:2208` | 改成 `数据口径与假设说明`（否则访客按助手说的名字去页面找 → 找不到） |
| 3 | **一个无出处的精确区间** | `index.html:3622`：`不同监测机构口径可能存在3-5%差异` | 改成 `差异可达数个百分点`，或补可核验出处（其余各行都已软化 ✓） |
| 4 | **品牌写法两种并存 + 括号后多余空格** | `:2194` 全称、其余简称；`:1604`/`:3135`/`:3665`/`:4354`/`:4362`/`:4415` 的 `）` 后多一个空格 | 定规则：**首现全称、其余简称**；顺手删掉 `）` 后的空格 |
| 5 | `.preview/` 未跟踪也未忽略 | `git status` → `?? .preview/` | 提交或写进 `.gitignore` |

## ⚠️ 三、流程状态（仍未处理）

- **本次改动还没提交**（`index.html`、`DUAL_AGENT_DISCUSSION.md` 在工作区）
- **master 仍未批准**：`check_review_stamp.py` → **exit 1**（最新批准行仍是 `814f782`，而 PR #5/#6 已并入 master）→ §33-A 的流程项还没做，master 上 CI 应仍为红
- 本地 `master` 有我的 3 个报告提交未推（报告豁免，推送不会被拦）

**建议顺序**：① 修上面 1–4 → ② `git add index.html CHANGELOG.md(如需) && git commit` → ③ **批准行写成这次提交的 sha**（一条行即可涵盖 PR #5/#6 + 本次改动，因为"内容相等"判据比的是 final 内容）→ ④ `check_review_stamp.py` 变绿 → ⑤ push → ⑥ 顺手 `.preview/` 与 GitHub **required status checks**

## 📌 保留这个做法（§35 里我提过，你做到了）
- **口径表 > 企业级数据字典**：1 天成本、每条都能对回公开来源（央行/统计局/金融监管总局/PCAOB/AICPA/COBIT）
- **"已知偏差或假设"列是差异化**：写清"数据在什么情况下会不准"，面试时直接当"我怎么保证结论可信"的答案
- **可核验性三档标记**（✅ 公开可查 / ⚠️ 第三方研究 / ⚠️ 行业估算）= 自带可信度分级

## 读报告
```bash
git show HEAD:REVIEW_REPORT_ACTIONS.md
git show HEAD:REVIEW_REPORT_v6.md | sed -n '4624,4689p'   # §36 完整复核
```

---

*复核AI（DeepSeek Harness）生成。本轮：口径表落地 ✅（5 作品 / 6 列 / 结构校验通过）；剩 5 处小残留 + master 未批准。*
