---
id: PF-RV-20260924-21
date: 2026-09-24T00:51:11
topic: gate_self红线修复
status: pending
diff_hash: ffb9979770001ba9
decision_ids: []
issue_ids: []
task_ids: []
phase: scheme
feature: F-20260924-02
commits: []
decision_maker: 用户
reviewers: [DeepSeek, 豆包, 用户]
model: deepseek-v4-flash
raw_file: rv-20260924-21-2170f05a33e3.json
raw_hash: sha256:11fcbb889ac46beca45ef0e6ba9e0770deb15ceb9a5a803c893b105abf49214f
supersedes: []
superseded_by: []
provenance: live
---
# PF-RV-20260924-21 · gate_self红线修复

- 时间：2026-09-24T00:51:11
- 阶段：scheme（方案评审·事前对齐）
- 功能登记：F-20260924-02
- 请求人：豆包（执行）｜决策：用户｜顾问：DeepSeek
- 原始响应：/home/user/Doubao/chats/38443714366494466/ai-finance-portfolio/tools/raw/rv-20260924-21-2170f05a33e3.json（sha256:11fcbb889ac46beca45ef0e6ba9e0770deb15ceb9a5a803c893b105abf49214f）

## 问题

【方案评审 · gate_self 红线覆盖过宽修复】tools/raw/（评审原始响应目录）被 gate_self 的 "tools/" 前缀误圈入红线，导致评审档案状态提交被三闸门误拦。

## 问题
- gate_rules.yaml 中 gate_self.paths 含 "tools/"（前缀匹配）
- tools/raw/ 存放 DeepSeek 评审原始响应 JSON（评审档案的一部分，非门禁代码）
- 结果：提交评审档案状态（如 rv-20260924-20.json）时被 classify 命中 gate_self，要求 diff_hash 匹配已批准评审 → 档案提交死锁（档案更新永远无法入库）

## 修复方案
- gate_self.paths 从 "tools/" 改为精确列举门禁代码文件：
  - tools/trace_gate.py
  - tools/deepseek_gate.py
  - tools/deepseek_client.py
  - tools/preflight.py
  - tools/check_review_stamp.py
 …

## DeepSeek 结论（原文节选）

**有条件批准**。问题诊断成立、修复方向正确，但方案在"匹配语义""清单完备性""证据完整性"三处存在未闭合项，需补齐后方可放行。不建议按当前文本直接实施。
1. 根因定位准确：`gate_self.paths` 的前缀匹配把 `tools/raw/`（评审证据）吞进"门禁自身"，造成档案状态提交的自锁——这是典型的**证据与控制流耦合**缺陷。
2. 修复方向正确：门禁红线应覆盖**控制面**（gate/CI/hook/规则文件），不应覆盖**证据面**（评审原始响应）。
3. 自指风险已被识别：本修复触碰 `gate_rules.yaml` 本身，属 gate_self 变更，须本评审批准并带 diff_hash 提交，方案已声明。
需明确 `gate_rules.yaml` 的路径匹配是前缀 / glob / 精确 / 支持否定排除。若支持 glob，**优先用 `tools/*.py` + `tools/*.yaml` 这类单层匹配**，而非逐文件枚举：`tools/raw/` 天然不命中，且新增门禁脚本不会漏网。逐文件枚举是"抗误伤、不抗漂移"的方案，只有在匹配语义退化到只支持精确串时才是唯一选择。
- 需给出门禁自身所依赖文件的完整候选清单，并说明如何确认 `tools/` 下**没有第 7、8 个门禁脚本**被漏掉；
- 需 grep 验证：`tools/` 相关判定逻辑（classify / preflight / check_review_stamp）中**不存在代码内硬编码的 `"tools/"` 前缀**。若存在，只改 YAML 无效，修复是个假闭环。
**B3. 排除 `tools/raw/` 后证据可篡改性未被兜底**

## 采纳状态

pending（决策人：用户）


---

## 阻断项闭环（DeepSeek 有条件批准，2026-09-24）

### B1 匹配语义
- gate_rules.yaml 的 paths 为**前缀匹配**（_classify 用 startswith 判断）
- 选型：**精确路径列表**（非 glob）。理由：glob `tools/*.py` 会误圈 make_icon_datauri.py/verify_deploy.py 两个辅助工具（非门禁）；精确列表抗误伤，配合 B2 清单证明完备性

### B2 清单完备性 + 无硬编码
- tools/ 顶层共 9 文件：6 门禁脚本（trace_gate/deepseek_gate/deepseek_client/preflight/check_review_stamp/project_state）+ gate_rules.yaml + 2 辅助工具（make_icon_datauri/verify_deploy，未被门禁/CI 引用）
- 硬编码核查：仅 gate_rules.yaml 含 "tools/" 前缀（本次修改目标）；trace_gate.py 的 _classify 用 startswith 读 yaml 规则，无独立硬编码
- 修复后 gate_self.paths = [tools/trace_gate.py, tools/deepseek_gate.py, tools/deepseek_client.py, tools/preflight.py, tools/check_review_stamp.py, tools/project_state.py, tools/gate_rules.yaml, .githooks/, .github/workflows/, PROJECT_BRIEF.md, REVIEW_STAMP.md, .gitignore]

### B3 证据可篡改补偿（append-only 约定）
- tools/raw/ 约定为 **append-only**：只新增评审响应文件，禁止原地改写已有文件；必要时以新文件+索引记录替代
- 评审档案（trace/03-会议与日志/DeepSeek评审/*.md）仍受三闸门保护（该目录不在本次排除范围，默认不属红线但受档案=权威源约束）
- 本约定写入 F-20260924-02 登记 + 本档案

### 非阻断建议采纳
- .gitignore 保留在红线（影响 trace/ 排除出 Pages 构建，理由成立）
- .githooks/ .github/workflows/ 目录前缀保留（整目录控制面，无豁免子目录）
- 防漂移：新增 tools/ 顶层门禁文件时同步更新 gate_self.paths（写入本档案约定）
